// Analytics stub — replace with real provider (Mixpanel, Amplitude, etc.) when ready

export function track(_event: string, _properties?: Record<string, unknown>) {
  // no-op
}

export function trackScreen(_screen: string, _tab?: string) {
  // no-op
}
